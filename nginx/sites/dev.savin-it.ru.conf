server {
    listen 80;
    listen [::]:80;
    server_name dev.savin-it.ru localhost;

    root /var/www/react-tables;
    index index.html;

    access_log /var/log/nginx/dev.savin-it.ru.access.log;
    error_log /var/log/nginx/dev.savin-it.ru.error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://sheets-backend:8000/;  # ✅ правильный сервис
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /health {
        proxy_pass http://sheets-backend:8000/health;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}